# use c() to create a vector
# create numeric vector
numeric_vector <- c(4, 7565, 15, 93,22, 100, 50, 0)

# display numeric vector
numeric_vector

# create character vector
char_vector <- c("A", "C", "L", "M", "O")

# display character vector
char_vector

# use c() to concatenate multiple vectors
# define two vectors
vec1 <- c(4, 15, 19, 18)
vec2 <- c(10, 100, 40, 20, 80, 85)

# concatenate vectors into one
vec3 <- c(vec1, vec2)

# view concatenatd vector
vec3

# use c() to create columns in data frame
# create data frame with three columns
df <- data.frame(team=c("A", "B", "C", "D", "E"),
                 points=c(99, 90, 86, 88, 95),
                 assists=c(33, 28, 31, 39, 34))

# view data frame
df












